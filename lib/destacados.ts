/**
 * Ids de producto (`${origen}-${codigo}`) para la sección "Lo que más me piden
 * esta campaña" de la landing. Vacío por defecto: sin esta lista, la página
 * elige sola los mejor calificados y disponibles — así el sitio nunca se ve
 * vacío, pero la selección real (qué de verdad se vende más) es tuya.
 */
export const idsDestacados: string[] = [];
