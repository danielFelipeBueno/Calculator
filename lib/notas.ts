/**
 * Notas personales de la consultora sobre productos puntuales — lo que la
 * distingue de un catálogo genérico ("El que más regalan para hombre",
 * "Yo lo uso de noche"). Vacías a propósito: son opinión suya, no algo que
 * se pueda inventar por ella. Se muestran solo en los productos que tengan
 * una entrada aquí; el resto del catálogo funciona igual sin ellas.
 *
 * La clave es el `id` del producto (`${origen}-${codigo}`, ver lib/productos.ts).
 */
export const notasConsultora: Record<string, string> = {
  // "yanbal-2213": "Un clásico. Si ya lo usaste, sabes por qué vuelve.",
};
